<%= simple_form_for(@lead) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :email %>
    <%= f.input :country %>
  </div>

  <div class="form-actions">
    <%= f.submit "Download", class: 'bttn-style' %>
  </div>
<% end %>
